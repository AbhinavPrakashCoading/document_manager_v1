{
  "title": "IELTS Document Requirements",
  "type": "object",
  "required": ["personalInfo", "documents"],
  "properties": {
    "personalInfo": {
      "type": "object",
      "required": ["name", "dateOfBirth", "passportNumber"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Full name as per passport"
        },
        "dateOfBirth": {
          "type": "string",
          "format": "date",
          "description": "Date of birth in YYYY-MM-DD format"
        },
        "passportNumber": {
          "type": "string",
          "description": "Valid passport number"
        }
      }
    },
    "documents": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["type", "file"],
        "properties": {
          "type": {
            "type": "string",
            "enum": ["photo", "signature", "passport", "identityProof"],
            "description": "Type of document"
          },
          "file": {
            "type": "string",
            "format": "binary",
            "description": "The document file"
          }
        }
      }
    }
  }
}
